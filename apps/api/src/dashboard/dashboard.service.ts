import { Injectable } from '@nestjs/common'; import { ConfigService } from '@nestjs/config'; import { PrismaService } from '../prisma/prisma.service.js'; import type { Environment } from '../config/environment.js';
@Injectable() export class DashboardService { constructor(private readonly prisma:PrismaService,private readonly config:ConfigService<Environment,true>){} async summary(tenantId:string){const cutoff=new Date(Date.now()-this.config.getOrThrow('IOT_OFFLINE_THRESHOLD_SECONDS')*1000);const [fleet,riders,active,online,seen]=await Promise.all([this.prisma.fleet.groupBy({by:['status'],where:{tenantId,deletedAt:null},_count:true}),this.prisma.rider.groupBy({by:['status'],where:{tenantId,deletedAt:null},_count:true}),this.prisma.allocation.count({where:{tenantId,status:'ACTIVE'}}),this.prisma.vehicleCurrentState.count({where:{tenantId,lastHeartbeat:{gte:cutoff}}}),this.prisma.vehicleCurrentState.count({where:{tenantId}})]);return {fleet:Object.fromEntries(fleet.map(x=>[x.status,x._count])),riders:Object.fromEntries(riders.map(x=>[x.status,x._count])),activeAllocations:active,iot:{online,offline:seen-online}};}}
